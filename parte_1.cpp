  
#include <iostream>
#include <fstream>
using namespace std;

int main(){
    
    float res_a ;
    float res_b ;
    float delta_x = 0.1;
    float w = 0.1;
    float y_0 = 1;
    
    for (int i = 0, i < 4/w, i++){
        res_a = (y_0) + (delta_x * w * y_0);
        res_b = (y_0) / (1 + delta_x * w);
    }
    )
    
    
    return 0;
}